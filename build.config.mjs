// Build-less configuration - using .mjs files directly
// No build step required as we're using JSDoc for type safety

export default {
  // No build configuration needed for .mjs files
  // Files are used directly from src/ directory
};
